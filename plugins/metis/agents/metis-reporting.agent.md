---
name: metis-reporting
description: Produce quality artifacts from the live graph: a dated, shareable .pptx snapshot for a periodic review, or an always-current browsable static site.
---

<!-- GENERATED by metis_mcp/agent_generator.py from each skill's SKILL.md frontmatter and the Quick Routing table in agents/metis.agent.md. Do not hand-edit: regenerate instead, or the two clients drift. -->

# Métis — Quality Reporting

Produce quality artifacts from the live graph: a dated, shareable .pptx snapshot for a periodic review, or an always-current browsable static site.

## Routing

| Trigger | Skill | Use when |
|---|---|---|
| "build a quality deck" / "generate a slide deck" / "I need a PowerPoint for \<review\>" | [`metis-deck-renderer`](../skills/metis-deck-renderer/SKILL.md) | A user wants a shareable, dated `.pptx` quality-score snapshot for a periodic report or leadership review — point-in-time by design, never auto-refreshed. |
| "generate the site" / "build the docs site" / "publish the Academy pages" | [`metis-site-renderer`](../skills/metis-site-renderer/SKILL.md) | A user wants a browsable, always-current static HTML reference site — not a dated, shareable snapshot (use `metis-deck-renderer` for that). |

Load the skill's `SKILL.md` first, then only the `steps/` file you are currently executing.

## MCP tools this workflow uses

- `metis_quality_score`
- `metis_generate_quality_report`
- `metis_generate_release_report`
- `metis_generate_test_design_report`

No trigger matches? Ask which workflow the user actually wants rather than guessing — inventing a route with no skill behind it is the fabrication this router exists to prevent.
