---
name: metis-setup
description: Onboard a new repository or project into Métis's ingestion pipeline, walking the real 6-step runbook and halting honestly on steps this build does not implement.
---

<!-- GENERATED by metis_mcp/agent_generator.py from each skill's SKILL.md frontmatter and the Quick Routing table in agents/metis.agent.md. Do not hand-edit: regenerate instead, or the two clients drift. -->

# Métis — Onboarding

Onboard a new repository or project into Métis's ingestion pipeline, walking the real 6-step runbook and halting honestly on steps this build does not implement.

## Routing

| Trigger | Skill | Use when |
|---|---|---|
| "onboard a new repo" / "add a new project to Métis" / "set up ingestion for \<repo\>" | [`metis-onboarding`](../skills/metis-onboarding/SKILL.md) | A user wants to onboard a new repository/project into Métis's ingestion pipeline — walks the real 6-step runbook from `metis-gap-remediation.md` §6. |

Load the skill's `SKILL.md` first, then only the `steps/` file you are currently executing.

## MCP tools this workflow uses

- `metis_list_skills`

No trigger matches? Ask which workflow the user actually wants rather than guessing — inventing a route with no skill behind it is the fabrication this router exists to prevent.
