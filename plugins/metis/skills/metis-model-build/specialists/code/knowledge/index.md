<!-- GENERATED by `metis knowledge` from the module docstring named in this skill's frontmatter. Do not hand-edit: the source of truth is the docstring, and `metis knowledge --check` fails on a diff. -->

# Knowledge index — metis-model-build-code

**Inherited from `metis-model-build`** — already in context when this
specialist runs, so it is not copied here:

- `model_sources-landing` — from `metis_mcp/model_sources/landing.py`
- `ontology-labels` — from `metis_mcp/ontology/labels.py`

Nothing further is extracted for this specialist: everything it
declares is its parent's, which is what makes it a specialist rather
than a separate skill.
