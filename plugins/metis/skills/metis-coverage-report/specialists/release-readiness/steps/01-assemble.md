# 01 — assemble what is known

## Actions

1. `coverage_report` for the scope: coverage, validation, reconciliation,
   version, and `unmeasured` with each cause and kind.
2. `metis drift` for publication drift — and record whether the ledger can see
   published content at all.
3. Where a claim needs justification, `trace` it and report the break.

## Forbidden substitutions

- Do not fill an unmeasured figure with an estimate.
- Do not average across figures of different kinds to make one number.

## Drift check

Every figure should belong to the scope asked about.

## Report

Each figure with its state, and what caps confidence.
