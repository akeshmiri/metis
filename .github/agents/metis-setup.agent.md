---
name: Métis — Onboarding
description: "Onboard a new repository or project into Métis's ingestion pipeline, walking the real 6-step runbook and halting honestly on steps this build does not implement."
argument-hint: "onboard a new repo"
tools:
- metis_list_skills
---

<!-- GENERATED by metis_mcp/agent_generator.py from each skill's SKILL.md frontmatter and the Quick Routing table in agents/metis.agent.md. Do not hand-edit: regenerate instead, or the two clients drift. -->

# Métis — Onboarding

Onboard a new repository or project into Métis's ingestion pipeline, walking the real 6-step runbook and halting honestly on steps this build does not implement.

## Workflows

### metis-onboarding

**Triggers:** "onboard a new repo" / "add a new project to Métis" / "set up ingestion for \<repo\>"

**Use when:** A user wants to onboard a new repository/project into Métis's ingestion pipeline — walks the real 6-step runbook from `metis-gap-remediation.md` §6.

Walk through metis-gap-remediation.md §6's real 6-step onboarding runbook for adding a new project/repository to Métis — checks real config (classification_gate.py, .metis/config.yaml), halts honestly on steps this build doesn't yet implement (calibration batch, Tree-sitter coverage), rather than faking a pass. Use when a user wants to onboard a new repository/project into Métis's ingestion pipeline.

**Stages** (execute in order, one at a time):
1. `01-test-id-convention.md`
2. `02-classification.md`
3. `03-language-coverage.md`
4. `04-calibration-batch.md`
5. `05-human-review.md`
6. `06-enable-ingestion.md`

The full text of each stage lives in `plugins/metis/skills/metis-onboarding/steps/`. Copilot does not load these automatically — open the file for the stage you are executing before acting on it.

## Ground rules

- Every claim must trace to a real graph entity or a real file. If the evidence is not there, say so — do not infer it.
- Métis entities land at `Quarantine` until a human approves them; never present a quarantined item as approved.
