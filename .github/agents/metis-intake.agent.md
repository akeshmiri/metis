---
name: Métis — Intake
description: "Get a real source into Métis: extract evidence from Jira, Confluence, Swagger/OpenAPI, Zephyr Scale, source code or a database, normalize it to one Unified Intake Format document, land it as an Episode, and mine its Requirements and AcceptanceCriteria — everything arriving at Quarantine for human review."
argument-hint: "ingest this ticket"
tools:
- metis_mine_requirements
- metis_submit_episode
- metis_get_context
---

<!-- GENERATED by metis_mcp/agent_generator.py from each skill's SKILL.md frontmatter and the Quick Routing table in agents/metis.agent.md. Do not hand-edit: regenerate instead, or the two clients drift. -->

# Métis — Intake

Get a real source into Métis: extract evidence from Jira, Confluence, Swagger/OpenAPI, Zephyr Scale, source code or a database, normalize it to one Unified Intake Format document, land it as an Episode, and mine its Requirements and AcceptanceCriteria — everything arriving at Quarantine for human review.

## Workflows

### metis-intake-processor

**Triggers:** "ingest this ticket" / "pull \<source\> into Métis" / "extract requirements from \<Jira\|Confluence\|Swagger\|code\>"

**Use when:** A user wants a real source brought into Métis — extract to UIF, land it as an Episode, then mine Requirements/AcceptanceCriteria from it. Not for mining an Episode that already exists (call `metis_mine_requirements` directly).

Extract evidence from Jira, Confluence, Swagger/OpenAPI, Zephyr Scale, source code or a database, normalize it to one Unified Intake Format (UIF) document, and land it in the Métis graph as an Episode so its Requirements and AcceptanceCriteria can be mined. Use when a user wants to get a real source into Métis — not for mining an Episode that already exists (call metis_mine_requirements directly for that).

**Stages** (execute in order, one at a time):
1. `01-extract.md`

The full text of each stage lives in `plugins/metis/skills/metis-intake-processor/steps/`. Copilot does not load these automatically — open the file for the stage you are executing before acting on it.

## Ground rules

- Every claim must trace to a real graph entity or a real file. If the evidence is not there, say so — do not infer it.
- Métis entities land at `Quarantine` until a human approves them; never present a quarantined item as approved.
