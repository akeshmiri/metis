{
  "_comment": [
    "Mounted at METIS_CONFIG_PATH. Read by metis_mcp.mbt.graph_session.",
    "",
    "This carried `zdr`, `models`, `corpus`, `security` and `repositories`",
    "blocks that no module in this build reads -- v1 residue that made the",
    "file look like configuration when it was documentation. What remains is",
    "the graph block, which is the only thing anything opens.",
    "",
    "`password_env` NAMES the variable holding the secret; it never holds one.",
    "The literal `password` key that was here is refused outright by",
    "graph_session._password_from_file when the file is readable beyond its",
    "owner -- and a Kubernetes secret volume mounts 0644 by default, so this",
    "chart could not authenticate at all. Naming the variable removes the",
    "question rather than answering it (PLT-005)."
  ],
  "graph": {
    "neo4j": {
      "uri": "bolt://{{ .Release.Name }}-neo4j:7687",
      "user": "neo4j",
      "password_env": "METIS_NEO4J_PASSWORD"
    }
  }
}
