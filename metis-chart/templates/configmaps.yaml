{{/*
  The connector-manifests and adversarial-corpus ConfigMaps were removed with
  the components that mounted them (ingestion-worker and
  guardrail-corpus-runner). Both belonged to the v1 ingestion and guardrail
  stack, which the rebuild replaced; a ConfigMap nothing mounts is shipped
  weight that reads like a live feature.

  The mcp-server's configuration arrives through the metis-config secret
  (see secrets.yaml) and METIS_HOME, which stays the deployment's source of
  truth. Nothing else needs a ConfigMap yet — add one here when something does.
*/}}
